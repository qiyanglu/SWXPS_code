# Local archive

This directory preserves superseded scripts and historical experiment bundles
that are not part of the maintained project. Its contents are ignored by Git
except for this README.

Historical documentation that remains useful for understanding development
decisions belongs in `docs/history/` instead.
